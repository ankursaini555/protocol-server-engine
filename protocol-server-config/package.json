{
  "name": "protocol-server-config",
  "version": "1.0.0",
  "description": "",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node app.js && node app igm && node app nts && node app trv && node app fis"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "json-schema-ref-parser": "^9.0.9"
  }
}
